---
scope: django-python-generate-sbom full system
purpose: build-substrate + HTML/C4 diagrams + solution design doc + non-technical one-pager
altitude: feature
updated: 2026-07-03T13:01
---

- (constraint) PRD final: _bmad-output/planning-artifacts/prds/prd-django-python-generate-sbom-2026-07-03/prd.md — all FRs, NFRs, API contract binding
- (constraint) [ADOPTED] Design paradigm: modular monolith with service layer — from addendum/research
- (constraint) [ADOPTED] Scaffold: cookiecutter-django 2026.26.4 with Celery + Redis + PostgreSQL + S3 — from addendum
- (constraint) [ADOPTED] Async pipeline: Celery chain/group canvas, 8 phases, phases 4-7 parallel — from PRD FR-4.1/4.2
- (constraint) [ADOPTED] Storage triad: PostgreSQL (durable state), Redis (transient task state + cache), S3/MinIO (artifacts) — from PRD NFR-1.2
- (constraint) [ADOPTED] Auth: custom Api-Key header; org-scoped keys; PBKDF2-hashed storage — from PRD F2
- (constraint) [ADOPTED] Multi-tenancy: org FK on all data models; task IDs prefixed org_id:task_id in Redis — from PRD NFR-1.1/1.2
- (constraint) [ADOPTED] Deployment: Docker Compose self-hosted; MinIO for local S3 dev — from PRD NFR-5.1
- (decision) Frontend: React + MUI v6 + Vite, full SPA served from Django via WhiteNoise; all data via REST API; frontend lives in frontend/ directory
- (decision) Dependency graph UI: replace PyVis HTML with React-native graph library; no iframes; backend Phase 6 emits JSON (nodes+edges) + Graphviz SVG instead of PyVis HTML
- (decision) Graph library: Cytoscape.js via react-cytoscapejs + dagre layout; hierarchical DAG view; backend graph API returns {nodes, edges} JSON; Graphviz SVG unchanged for download
- (decision) Org isolation: application layer (not DB-level RLS); pattern TBD in coaching
- (decision) Org isolation pattern: OrgScopedModel abstract base (org FK) + OrgScopedQuerySet.for_org(org) + explicit org param in all service functions; views extract org from API key and pass explicitly; 404 for cross-org and non-existent objects via single queryset path
- (decision) Celery queue topology: two queues — 'pipeline' (phases 1-3, phase 8) and 'analysis' (phases 4-7); separate worker pools; prevents vuln scan starvation of new job submissions
- (decision) Per-org concurrency enforcement: database count check via SBOMJob.objects.for_org(org).filter(status__in=['PENDING','PROGRESS']).count() before enqueue; race condition acceptable at target scale; simpler than Redis atomic counter which risks counter drift on task crash
- (decision) API key implementation: djangorestframework-api-key via AbstractAPIKey subclass (OrgApiKey adds org FK, last_used_at, revoked_at); library handles crypto (SHA-512, appropriate for random tokens), generation, prefix, DRF auth; ~20 lines of customisation for last_used_at update and org ownership check
- (event) ARCHITECTURE-SPINE.md distilled; all versions verified against PyPI and npm
- (change) AD-2: clarified 404 (API) vs 403 (web UI) distinction for cross-org access
- (change) AD-4: Beat cleanup tasks routed to pipeline queue
- (change) AD-7: Binds clarified — manifests/views.py owns generate endpoint, creates ManifestUpload + SBOMJob
- (change) AD-9: tightened with exact Cytoscape.js node/edge JSON schema including data wrapper
- (decision) AD-11: artifact downloads via presigned S3/MinIO URL (303 redirect); Django never proxies artifact bytes
- (decision) AD-12: SBOMJob.status written exclusively by Celery task code via sbom/services.py; views read-only except initial PENDING at creation
- (change) Conventions: added org access (request.auth.org), storage paths, chord envelope, artifact cleanup, pagination, health check
- (change) Stack: pygraphviz corrected from 1.14 to 2.0
- (change) PRD FR-5.3 and FR-6.5 updated: PyVis replaced with Cytoscape.js + dagre
- (event) ARCHITECTURE-SPINE.md finalized (status: draft → final); all 12 ADs binding
- (artifact) solution-design.md written — full module breakdown, API inventory, storage paths, Celery canvas, frontend architecture, Docker Compose, configuration table
- (artifact) one-pager.md written — non-technical product overview
- (artifact) architecture-diagrams.html written — self-contained HTML with 8 interactive Mermaid diagrams: System Context (C4 L1), Container (C4 L2), Component (C4 L3), Async Pipeline (sequence), Data Model (ERD), Module Dependency Direction, Arch Decision Summary (all 12 ADs), Verified Stack table; dark theme, nav with scroll-highlighting
- (decision) AD-13: monorepo layout — backend/ and frontend/ are project-root peers; manage.py, pixi.toml, pyproject.toml live under backend/; docker-compose.yml and LICENSE at project root; prevents Django scaffold at project root or toolchains sharing a working directory
- (decision) AD-13 amended (build-time): pixi is the umbrella toolchain for the WHOLE project, not backend-only. Single root pixi.toml installs Python env + Node runtime (conda-forge nodejs, pinned in pixi.lock) and orchestrates both backend (cwd=backend) and frontend (cwd=frontend, shells to npm/vite). backend/ keeps pyproject.toml + manage.py; frontend/ keeps package.json (npm manages JS deps). Unified pixi run ci gates both. Supersedes prior 'pixi.toml lives under backend/, npm independent' rule. Decided by Kevin during Story 1.1 build.
