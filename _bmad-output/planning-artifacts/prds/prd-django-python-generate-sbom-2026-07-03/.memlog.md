---
topic: django-python-generate-sbom PRD
updated: 2026-07-03T10:43
---

- (decision) Auth: API key only in v1; OAuth deferred
- (decision) Multi-tenancy: GitHub-model — users can belong to multiple orgs; orgs are fully isolated
- (decision) Artifact retention: 10-day max TTL for SBOMs and all analysis reports
- (decision) UI-first: web UI with results on separate tabs; inline dependency graph; job history dashboard
- (decision) v1 manifest formats: requirements.txt, pyproject.toml, pixi.lock, pixi.toml, conda environment.yml (best-effort)
- (decision) v2 deferred: uv.lock, poetry.lock manifest formats; OAuth; SPDX 3.0
- (decision) Stakes: hobby/learning project targeting OSS release; moderate PRD rigor
- (assumption) OSS license: Apache 2.0 assumed; Kevin to confirm
- (assumption) Deployment: self-hosted via Docker Compose; no Kevin-hosted public instance in v1
- (assumption) API keys are org-scoped; one key grants access to that org's resources
- (assumption) Org admin invites members via email; invitation model TBD
- (decision) License: Apache 2.0 (switched from MIT); reason: patent grant clause benefits enterprise adoption for compliance tooling
- (decision) conda/mamba is a required runtime dependency; conda environment.yml support is not best-effort; job fails with descriptive error if solver absent
- (decision) License compliance report: four-tier model — Strong Copyleft (AGPL/GPL, attention required), Weak Copyleft (LGPL, review recommended), Unknown (no/non-SPDX license), Permissive
- (decision) Per-org concurrent job limit: configurable via SBOM_MAX_CONCURRENT_JOBS_PER_ORG env var, default 5
- (decision) Org member invitation: admin creates account directly (email + temp password); no email infrastructure required
- (decision) UI job status refresh: 5-second JavaScript polling; WebSocket/Django Channels deferred to v2
- (change) FR-3.5: added estimated_seconds to 202 response per research API contract
- (change) FR-5.1: added CWE classification (NVD enrichment) to vulnerability report
- (change) FR-5.4: LTS version registry made operator-configurable via SBOM_LTS_REGISTRY env var
- (change) FR-4.3: corrected pixi.lock parser from TOML to PyYAML (pixi.lock is YAML-formatted)
- (change) FR-4.6: specified SoftTimeLimitExceeded behavior — FAILED, no partial SBOM, reason surfaced to user
- (change) FR-5.4: version currency classification redefined by release series distance, handles major-version gaps
- (change) API table: corrected member endpoint label to 'Create member account'; added GET /members/ list endpoint
- (change) FR-2.2: removed stale [ASSUMPTION] tag; 10-key limit is decided
- (event) PRD finalized
