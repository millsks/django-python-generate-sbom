"""The django_service host-project package.

Holds what belongs to *this* deployment rather than to a reusable app: the
concrete `User` model (Story 21.2), project-wide templates/static/media, and
project-level helpers. `APPS_DIR` in the settings points here.
"""
